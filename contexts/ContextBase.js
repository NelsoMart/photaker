
import React from 'react';

// Prototipo Context

export default React.createContext({

    //Camera utilities
    camera: null, //? it is to states
    cameraRegister: () => {} //? it is for functions

}) 